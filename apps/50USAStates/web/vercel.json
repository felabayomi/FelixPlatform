{
    "framework": null,
    "installCommand": "npm install",
    "buildCommand": "npm run build",
    "outputDirectory": "dist/public",
    "rewrites": [
        {
            "source": "/((?!assets/|favicon|manifest|uploads/).*)",
            "destination": "/index.html"
        }
    ]
}